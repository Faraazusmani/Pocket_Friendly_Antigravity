# Profiles, Security & Settings

Multiple independent local profiles are supported.

One app-wide lock protects the application using PIN/device authentication where available.

Privacy Mode hides monetary values without changing layout.

Settings include notifications, theme, currency, app lock, privacy, export, import, profiles, help and rating.

Member since displays year only. Profile image remains local and is excluded from import/export.
